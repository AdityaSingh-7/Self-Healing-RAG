# Web framework
fastapi[standard]>=0.115.0
uvicorn>=0.30.0

# Settings management (loads .env files)
pydantic-settings>=2.5.0
python-dotenv>=1.0.1

# Document parsing
pymupdf>=1.24.0
python-docx>=1.1.0

# RAG pipeline (using à la carte components)
llama-index-core>=0.11.0

# Embeddings + Reranking
sentence-transformers>=3.0.0
numpy>=1.26.0

# Cross-encoder reranking (two-stage retrieval)
# Uses sentence-transformers CrossEncoder class
# Model: cross-encoder/ms-marco-MiniLM-L-6-v2 (auto-downloads ~80MB)

# Vector database
pinecone>=5.0.0
pinecone-text>=0.9.0

# LLM inference (Groq + Llama 3)
groq>=0.9.0

# Auth - JWT token verification
python-jose[cryptography]>=3.3.0

# File upload handling
python-multipart>=0.0.9

# Evaluation (install separately when needed)
# ragas>=0.2.0
# datasets>=2.20.0
